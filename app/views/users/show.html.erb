<% provide(:title, @user.username) %>

<div class="col-md-10">
	<h1 class="text-center">IMAGINE BOARD</h1>
	<hr>
	<h3>All pictures posted by <strong><%= @user.username %>(<%= @user.images.count%>)</strong></h3>
</div>

<%= render 'shared/images' %>