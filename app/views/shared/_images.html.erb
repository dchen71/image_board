<div class="col-md-10">

	<%= render 'shared/errors' %>
	
	<div class="image-board">
		<% unless @images.nil? %>
			<% @images.each do |i| %>
				<%= link_to image_tag(i.pictures.url),image_path(i.id)  %>
			<% end %>	
		<% end %>
	</div>
</div>
