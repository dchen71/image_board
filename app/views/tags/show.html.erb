<% provide(:title, @tag.tag) %>

<div class="col-md-10 col-xs-10">
	<h1 class="text-center">IMAGINE BOARD</h1>
	<hr>
	<h3>All pictures tagged by <strong><%= @tag.tag %>(<%= @images.count%>)</strong></h3>
</div>

<%= render 'shared/images' %>